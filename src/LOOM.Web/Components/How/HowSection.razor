<section class="how" id="how">
    <div class="reveal">
        <h2 class="section-title">
            From <em>blank canvas</em> to deployable automation<br />
            in a focused loop.
        </h2>
    </div>

    <div class="how-steps reveal-stagger">
        <div class="how-step">
            <div class="num">STEP 01</div>
            <h4>Map the workflow</h4>
            <p>Add inputs, API calls, transforms, conditions, and outputs from the node library.</p>
        </div>
        <div class="how-step">
            <div class="num">STEP 02</div>
            <h4>Wire them up</h4>
            <p>Connect output ports to input ports. Cycles and type mismatches are blocked instantly with a diagnostic.</p>
        </div>
        <div class="how-step">
            <div class="num">STEP 03</div>
            <h4>Run the graph</h4>
            <p>One click executes the graph, highlights each visited node, and shows outputs inline.</p>
        </div>
        <div class="how-step">
            <div class="num">STEP 04</div>
            <h4>Export and ship</h4>
            <p>Generate clean C# source, commit it, and deploy through your existing .NET pipeline.</p>
        </div>
    </div>
</section>
